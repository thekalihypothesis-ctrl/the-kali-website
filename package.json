{
  "name": "kali-hypothesis",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy --autoconfig=false"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
