---
import MainLayout from '../layouts/MainLayout.astro';
import { hotspots } from '../data/hotspots.js';

// Same content as the office popup (see hotspots.js) — this page is
// what the cat-mask hotspot and any direct /story link land on, so it
// stays in sync with the popup instead of carrying its own copy.
const { note } = hotspots.find((spot) => spot.id === 'story');
---
<MainLayout title={note.title}>
  <div class="case-file">
    <a class="back-link" href="/">&larr; Back to the office</a>
    <h1>{note.title}</h1>
    {note.body.map((paragraph) => <p>{paragraph}</p>)}
  </div>
</MainLayout>

<style>
  .case-file {
    position: relative;
    width: 100%;
    height: 100%;
    padding: 80px 117px;
    background: #0a0a0a;
    color: #f5f5f0;
  }

  .back-link {
    display: inline-block;
    margin-bottom: 40px;
    color: #f5f5f0;
    text-decoration: none;
    border-bottom: 1px solid currentColor;
  }

  .back-link:hover {
    opacity: 0.7;
  }

  h1 {
    font-size: 43px;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    margin-bottom: 24px;
  }

  p {
    max-width: 800px;
    font-size: 16px;
    line-height: 1.6;
  }
</style>
