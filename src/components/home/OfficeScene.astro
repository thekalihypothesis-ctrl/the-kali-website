<section class="office-scene">
  <img
    src="/assets/scenes/desktop/home-bg.jpg"
    alt="Office scene"
    class="scene-image"
  />

  <div class="scene-overlay">
    <div class="intro-panel">
      <span class="scene-kicker">Kali Hypothesis</span>
      <h1>Enter the office. Follow the signal.</h1>
      <p>
        A noir world of cases, coded traces, exhibition fragments, field notes,
        and hidden layers that can be opened through web and AR.
      </p>

      <div class="cta-row">
        <a href="/story" class="button-link">Open Case File</a>
        <a href="/ar" class="pill-link">Launch AR Viewer</a>
      </div>
    </div>
  </div>

  <a href="/ar" class="monitor-hotspot" aria-label="Open AR Viewer"></a>
</section>

<style>
  :root {
    --navWidth: 220px;
  }

  .office-scene {
    position: relative;
    min-height: 100vh;
    background: #000;
    overflow: hidden;
    padding-left: var(--navWidth);
  }

  .scene-image {
    width: 100%;
    height: 100vh;
    object-fit: contain;
    object-position: center;
  }

  .scene-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: flex-end;
    justify-content: center;
    padding: 32px 24px 32px calc(var(--navWidth) + 24px);
    pointer-events: none;
  }

  .intro-panel {
    width: min(100%, 720px);
    border: 1px solid rgba(255, 255, 255, 0.16);
    border-radius: 24px;
    padding: 24px;
    background: rgba(0, 0, 0, 0.66);
    backdrop-filter: blur(8px);
    pointer-events: auto;
  }

  .scene-kicker {
    display: inline-block;
    margin-bottom: 10px;
    font-size: 12px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #cfcfcf;
  }

  .intro-panel h1 {
    margin: 0 0 12px;
    font-size: clamp(2rem, 4vw, 3.8rem);
    line-height: 1;
    color: #fff;
  }

  .intro-panel p {
    margin: 0;
    color: #c6c6c6;
    line-height: 1.6;
  }

  .cta-row {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
    margin-top: 18px;
  }

  .pill-link,
  .button-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 44px;
    padding: 10px 16px;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.18);
    background: rgba(255, 255, 255, 0.06);
    color: #fff;
    text-decoration: none;
    transition: background 0.2s ease, border-color 0.2s ease;
  }

  .pill-link:hover,
  .button-link:hover {
    background: rgba(255, 255, 255, 0.12);
    border-color: rgba(255, 255, 255, 0.3);
  }

  .monitor-hotspot {
    position: absolute;
    left: calc(var(--navWidth) + 41%);
    top: 41%;
    width: 16%;
    height: 18%;
    border-radius: 16px;
  }

  .monitor-hotspot:hover {
    box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.35) inset;
    background: rgba(255, 255, 255, 0.04);
  }

  @media (max-width: 900px) {
    :root {
      --navWidth: 0px;
    }

    .office-scene {
      padding-left: 0;
    }

    .scene-overlay {
      align-items: flex-end;
      padding: 16px;
    }

    .intro-panel {
      padding: 18px;
    }

    .monitor-hotspot {
      left: 32%;
      top: 40%;
      width: 36%;
      height: 18%;
    }
  }
</style>